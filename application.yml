# Application Name
  application:
    name: ConfigServer
    
# Eureka Discovery Register    
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
